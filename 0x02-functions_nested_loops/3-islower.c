#include "main.h"

/**
*_islower - checks if the letter lowercase or not
* Return: 1 or 0 for true , or false
* @c:single letter input
*/

int _islower(int c)
{
if (c >= 'a' && c <= 'z')

return (1);
else
return (0);
}
