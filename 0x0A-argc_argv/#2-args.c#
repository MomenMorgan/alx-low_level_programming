#include <stdio.h>

/**
 * main - prints the number of arguments passed into it.
 *@argc:input
 *@argv:input
 *Return: 0 success.
 */
int main(int argc  , char *argv[])
{
  while(argc--)
{
printf("%s",argv[argc]);

}
return (0);
}
